import torch
import math

def get_positional_encoding(seq_len, d_model):
    pass

if __name__ == "__main__":
    pe_matrix = get_positional_encoding(seq_len=10, d_model=16)
    print("Positional Encoding Shape:", pe_matrix.shape)